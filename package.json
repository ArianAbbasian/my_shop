{
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^6.0.1",
    "tailwindcss": "^3.4.17"
  },
  "scripts": {
  "build": "rimraf dist && mkdir dist && copyfiles -u 0 index.html img/**/* app.js dist/ && npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify"
}
}
